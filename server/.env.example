# Port to listen on (defaults to 3001)
PORT=3001

# Comma-separated list of allowed origins. Leave empty to allow all (dev only!).
# Example: ALLOWED_ORIGINS=https://velotype.example.com,https://www.velotype.example.com
ALLOWED_ORIGINS=
